extends layout

block content
  .container
    h1 Uso de Puntos
    .row
      .col-2
        label Buscar:
        select(class="form-control margin-bottom" id="concepto")
          option(value="", selected) --
          each canje in canjes
            option(value=canje.id) #{canje.descripcion}
      .col-2
        label(for="id_cliente") Cliente
          select(class="form-control margin-bottom" id="cliente")
            option(value="0", selected) --
            each cliente in clientes
              option(value=cliente.id) #{cliente.nombre}
      .col-2
        label Desde:
        input(id="desde",type="date",class="form-control")
      .col-2
        label Hasta:
        input(id="desde",type="date",class="form-control")
      .col-2
        button(id="btn-consultar", class="btn btn-default" style="position:absolute;bottom:0") Consultar
    br
    table(id="tabla-bolsas")
  script(src="/javascripts/bolsas.js")